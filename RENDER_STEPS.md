# Пошаговая инструкция по развертыванию на Render.com

## Шаг 1: Подготовка репозитория на GitHub

1. Убедитесь, что все файлы загружены на GitHub:
   - `server.py`
   - `faceit_elo_widget.html`
   - `faceit_widget.css`
   - `faceit_widget.js`
   - `requirements.txt`
   - `Procfile`
   - `render.yaml` (опционально)

2. Проверьте, что репозиторий публичный или у вас есть доступ к приватному репозиторию

---

## Шаг 2: Регистрация на Render.com

1. Зайдите на [render.com](https://render.com)
2. Нажмите "Get Started for Free"
3. Зарегистрируйтесь через GitHub (рекомендуется) или email
4. Подтвердите email (если регистрировались через email)

---

## Шаг 3: Создание Web Service

1. В Dashboard Render нажмите **"New +"** (вверху справа)
2. Выберите **"Web Service"**
3. Подключите ваш GitHub аккаунт (если еще не подключен):
   - Нажмите "Connect GitHub"
   - Разрешите доступ к репозиториям
   - Выберите репозиторий с виджетом

4. Выберите репозиторий с виджетом из списка

---

## Шаг 4: Настройка Web Service

Заполните форму:

### Основные настройки:

- **Name**: `faceit-widget` (или любое другое имя)
- **Region**: Выберите ближайший регион (например, Frankfurt, Germany)
- **Branch**: `main` (или `master`, в зависимости от вашей ветки)
- **Root Directory**: (оставьте пустым, если файлы в корне)

### Build & Deploy:

- **Environment**: `Python 3`
- **Build Command**: (оставьте **пустым**)
- **Start Command**: `python server.py`

### Advanced:

- **Auto-Deploy**: `Yes` (автоматически обновлять при push в GitHub)
- **Health Check Path**: (оставьте пустым)

---

## Шаг 5: Установка переменных окружения

**ВАЖНО!** Это критически важно для безопасности!

1. В разделе **"Environment"** нажмите **"Add Environment Variable"**

2. Добавьте следующие переменные:

   **Обязательные:**
   ```
   WIDGET_PASSWORD=ваш_сложный_пароль_минимум_32_символа
   ```

   **Рекомендуемые:**
   ```
   WIDGET_TOKEN=ваш_сложный_токен_минимум_64_символа
   ```

   **Автоматические (не нужно добавлять):**
   ```
   PORT=10000
   ```
   (Render автоматически устанавливает PORT)

3. **Генерация пароля и токена:**

   **Windows PowerShell:**
   ```powershell
   # Пароль (32 символа)
   -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | ForEach-Object {[char]$_})
   
   # Токен (64 символа)
   -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | ForEach-Object {[char]$_})
   ```

   **Linux/Mac:**
   ```bash
   # Пароль
   openssl rand -base64 32
   
   # Токен
   openssl rand -hex 32
   ```

4. **Скопируйте сгенерированные значения** и вставьте в Render

---

## Шаг 6: Создание сервиса

1. Нажмите **"Create Web Service"**
2. Render начнет деплой (это займет 2-5 минут)
3. Следите за логами в разделе "Logs"

---

## Шаг 7: Получение URL

После успешного деплоя:

1. В Dashboard Render найдите ваш сервис
2. Скопируйте **URL** (например: `https://faceit-widget.onrender.com`)
3. Сохраните этот URL - он понадобится для OBS

---

## Шаг 8: Использование в OBS

1. Откройте OBS Studio
2. Добавьте **"Browser Source"**
3. В настройках Browser Source:

   **Если используете токен:**
   - **URL**: `https://your-service.onrender.com/faceit_elo_widget.html?token=ваш_токен`
   
   **Если используете только пароль:**
   - **URL**: `https://your-service.onrender.com/faceit_elo_widget.html`
   - Включите **"Shutdown source when not visible"**
   - Добавьте HTTP Basic Auth:
     - Username: `widget` (или любое)
     - Password: `ваш_пароль`

4. **Размеры:**
   - Width: `800`
   - Height: `400`

5. Нажмите **"OK"**

---

## Шаг 9: Проверка работы

1. В OBS должен появиться виджет
2. Если виджет не загружается:
   - Проверьте логи в Render Dashboard
   - Убедитесь, что URL правильный
   - Проверьте токен/пароль

---

## Обновление виджета

После изменений в коде:

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Update widget"
   git push
   ```

2. Render автоматически обновит сервис (если Auto-Deploy включен)

3. Дождитесь завершения деплоя (2-5 минут)

---

## Устранение неполадок

### Виджет не загружается в OBS

1. **Проверьте логи в Render:**
   - Dashboard → Ваш сервис → "Logs"
   - Ищите ошибки

2. **Проверьте URL:**
   - Должен быть HTTPS
   - Должен содержать токен (если используете)

3. **Проверьте переменные окружения:**
   - Dashboard → Ваш сервис → "Environment"
   - Убедитесь, что `WIDGET_PASSWORD` установлен

### Ошибка 401/403

1. Проверьте токен/пароль
2. Убедитесь, что переменные окружения установлены правильно
3. Проверьте формат URL

### Сервис не запускается

1. Проверьте логи в Render
2. Убедитесь, что `Start Command` правильный: `python server.py`
3. Проверьте, что все файлы загружены на GitHub

---

## Важные замечания

⚠️ **Безопасность:**
- Никогда не коммитьте пароли/токены в Git
- Используйте сложные пароли (минимум 32 символа)
- Используйте токен для дополнительной защиты
- Регулярно обновляйте пароли/токены

⚠️ **Бесплатный план Render:**
- Сервис "засыпает" после 15 минут бездействия
- Первый запрос после "сна" может занять 30-60 секунд
- Для постоянной работы нужен платный план ($7/месяц)

⚠️ **HTTPS:**
- Render автоматически предоставляет HTTPS
- Используйте HTTPS URL в OBS

---

## Готово!

Теперь ваш виджет доступен в интернете и защищен паролем/токеном! 🎉

